# تحديث 9.7 — إصلاح صفحة القاعات

تم تنفيذ التعديلات التالية:

- إصلاح مشكلة اختفاء القاعة بعد الضغط على حفظ.
- تحويل حفظ القاعات إلى حفظ فوري ومؤكد عبر `persistNow` بدل الاعتماد على التحديث المحلي فقط.
- إضافة عمليات واضحة في `useRoomsData`:
  - `createRoom`
  - `updateRoom`
  - `deleteRoom`
  - `deleteAllRooms`
- إضافة عمليات فورية مماثلة في `useRoomBlocksData` حتى لا تضيع بيانات الحظر.
- تحسين صفحة `Rooms.tsx` بحيث:
  - تمنع الحفظ قبل اكتمال تحميل البيانات.
  - تعرض رسالة تحميل ورسالة خطأ واضحة.
  - تحفظ الإضافة والتعديل والحذف والاستيراد بشكل فعلي.
  - تحفظ الحظر السريع بشكل فوري.

## الملفات التي تم تعديلها

- `src/hooks/useRoomsData.ts`
- `src/hooks/useRoomBlocksData.ts`
- `src/pages/Rooms.tsx`

